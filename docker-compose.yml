services:
  app:
    image: azul/zulu-openjdk-alpine:21-jre-headless-latest
    ports:
      - ${HOST_PORT}:${HOST_PORT}
    env_file: .env
    volumes:
      - ./build/libs/recipefyapi-all.jar:/app/recipefy.jar
      - ./logs:/app/logs
    command: ["java", "-XX:+UseShenandoahGC", "-jar", "/app/recipefy.jar"]
    restart: unless-stopped